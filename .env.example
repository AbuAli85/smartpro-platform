# Copy to .env and fill in. Used by the server and by drizzle (db:push, migrations).

# MySQL connection string (required for db:push, migrate, and runtime).
# Example: mysql://USER:PASSWORD@HOST:3306/DATABASE
# Railway: use the MySQL connection URL from your Railway MySQL service variables.
DATABASE_URL=
